import React from 'react';

const PromiseForm = function(props) {
    const {classEnvelope} = props.classes || {}; 
    return (
        <form className={classEnvelope}>PromiseForm needs work.</form>
    );
}

export default PromiseForm;