import React from 'react';

const CreateNewFlakeyBtn = function(props) {
    const {classEnvelope} = props.classes || {}; 
    return (
        <button className={classEnvelope}>CreateNewFlakeyBtn needs work.</button>
    );
}

export default CreateNewFlakeyBtn;