import React from 'react';

const AddFlakeyKeyBar = function(props) {
    const {classEnvelope} = props.classes || {}; 
    return (
        <div className={classEnvelope}>AddFlakeyKeyBar needs work.</div>
    );
}

export default AddFlakeyKeyBar;