import React from 'react';

const DeleteFlakeyBtn = function(props) {
    const {classEnvelope} = props.classes || {}; 
    return (
        <button className={classEnvelope}>DeleteFlakeyBtn needs work.</button>
    );
}

export default DeleteFlakeyBtn;