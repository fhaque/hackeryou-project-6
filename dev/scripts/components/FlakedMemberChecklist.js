import React from 'react';

const FlakedMemberChecklist = function(props) {
    const {classEnvelope} = props.classes || {}; 
    return (
        <div className={classEnvelope}>FlakedMemberChecklist needs work.</div>
    );
}

export default FlakedMemberChecklist;